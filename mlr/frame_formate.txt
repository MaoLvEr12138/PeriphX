mcu -> fpga  && fpga -> mcu
{
    u8 head_magic: 0x55,
    u16 server_id,
    u8 msg_type,        // 0x00:request , 0x01:response , 0x02:event , 0x03:error 
    u32 payload         // payload长度和格式是约定好的,不需要len参数
}